package org.example;

public class App {
    public static void main(String[] args) {
        // TODO(your-name): Wire this entrypoint to:
        // 1) load AppConfig
        // 2) initialize SQLite schema
        // 3) construct repository + service + command/router layer
        // 4) execute CLI/HTTP command handling
        System.out.println("KV Store scaffold created. Fill TODOs in org.example.kvstore.*");
    }
}
